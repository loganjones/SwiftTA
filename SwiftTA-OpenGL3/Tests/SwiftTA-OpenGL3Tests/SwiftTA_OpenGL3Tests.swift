import XCTest
@testable import SwiftTA_OpenGL3

final class SwiftTA_OpenGL3Tests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftTA_OpenGL3().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
